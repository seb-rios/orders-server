export interface Orders {
  id: string;
  quantity: string;
  garmentId: string;
  styleId: string;
  createdAt: string;
  updatedAt: string;
  productionProcessId: string;
  clientId: string;
  reportId: string;
}
